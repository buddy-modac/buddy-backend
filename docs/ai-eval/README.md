# Buddy AI Eval Report

GitHub Pages용 정적 리포트입니다.

- 진입점: `index.html`
- 이미지 에셋: `assets/`
- 생성 명령:

```bash
node scripts/ai-eval.mjs \
  --results <result-json-csv> \
  --pages-dir docs/ai-eval
```

GitHub Pages source를 `docs/`로 설정하면 `/ai-eval/` 경로에서 볼 수 있습니다.
